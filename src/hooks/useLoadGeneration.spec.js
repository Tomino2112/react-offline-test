describe("useLoadGeneration", () => {
    test("should return correct state while loading", () => {});
    test("should return correct state when done", () => {});
    test("should return correct state when error", () => {});
});